# MODEL CONFIGURATION
users:
  mail : string
  name : string
  password : password
  level : 0
  _button : [submit, reset]
sessions:
  userid : string
  cookie : string
  time : string
news:
  user : { select : name, model : users}
  title : string
  content : wysiwyg
  description : text
  _button : [submit, reset]

